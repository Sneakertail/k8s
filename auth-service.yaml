apiVersion: v1
kind: ConfigMap
metadata:
  name: auth-service-config
  namespace: k8s
data:
  PORT: "3001"
  ADMIN_USERNAME: "admin"
  ADMIN_EMAIL: "admin@sneakertail.com"
  DEMO_USERNAME: "user"
  DEMO_EMAIL: "user@sneakertail.com"
---
apiVersion: v1
kind: Secret
metadata:
  name: auth-service-secret
  namespace: k8s
type: Opaque
stringData:
  JWT_SECRET: "replace-with-cluster-secret"
  ADMIN_PASSWORD: "replace-with-cluster-secret"
  DEMO_PASSWORD: "replace-with-cluster-secret"
---

apiVersion: v1
kind: Service
metadata:
  name: auth-service-active
  namespace: k8s
spec:
  selector:
    app: auth-service
  ports:
    - name: http
      port: 3001
      targetPort: 3001

---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service
  namespace: k8s
spec:
  replicas: 1
  selector:
    matchLabels:
      app: auth-service
  template:
    metadata:
      labels:
        app: auth-service
    spec:
      containers:
        - name: auth-service
          image: ghcr.io/sneakertail/auth-service:dev
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 3001
          envFrom:
            - configMapRef:
                name: auth-service-config
            - secretRef:
                name: auth-service-secret
          readinessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 300m
              memory: 256Mi
